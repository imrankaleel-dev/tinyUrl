/**
 * /api/*
 */
const createLinks = "/links";
const getAllLinks = "/links";
const getLinkStats = "/links/:code";
const deleteLink = "/links/:code";

module.exports = {
  createLinks,
  getAllLinks,
  getLinkStats,
  deleteLink,
};
